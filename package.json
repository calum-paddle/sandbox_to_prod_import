{
  "name": "paddle-sandbox-to-prod-migrator",
  "version": "1.0.0",
  "description": "Tool to migrate Paddle products from sandbox to production",
  "scripts": {
    "server": "cd backend && npm run dev",
    "client": "BROWSER=none cd frontend && npm start",
    "start": "concurrently \"npm run server\" \"npm run client\" \"npm run open-browser\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "open-browser": "sleep 4 && open http://localhost:3000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
